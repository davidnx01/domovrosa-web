import { TImage } from "@/lib/utils"

export type TGeneral = {
  logo: TImage;
  phone: string;
  email: string;
  address: string;
  city: string;
  post_code: string;
}

export type THeading = {
  subtitle: string;
  title: string;
}